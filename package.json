{
  "name": "lewefy-monorepo",
  "private": true,
  "workspaces": [
    "lewefyfrontend"
  ],
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd lewefyfrontend && npm run build",
    "dev": "cd lewefyfrontend && npm run dev",
    "lint": "cd lewefyfrontend && npm run lint"
  }
}

